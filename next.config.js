/** @type {import('next').NextConfig} */
const nextConfig = {
    
    images: {
        domains: [
            "res.cloudinary.com"
        ]
    },
    output: 'standalone'
}

module.exports = nextConfig
